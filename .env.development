# 开发环境配置
# 走 Vite 代理保持同源，避免 SameSite=Lax 阻断 Cookie（直连 localhost:3000 会因跨站被拒收）
VITE_API_BASE_URL=/api

# 开发服务器配置
VITE_DEV_HOST=financial-news.net
VITE_DEV_PORT=5173
VITE_PROXY_TARGET=http://localhost:3000
